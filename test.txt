        <!-- <h1 class = "text-center">Guess this word!</h1>
        <div id = "words" class = "text-center"></div>

        var fruits = ["apple", "orange", "banana"];
var wins = 0;
var losses = 0;
var errors = [];

// Random fruits appear
var randomWord = fruits[Math.floor(Math.random() * fruits.length)];

// console.log(randomWord);
var wordsElement = document.getElementById("words");

var newWord = document.createElement("p");

newWord.textContent = randomWord;

var eWord = "";

// 1. Turn words into blanks.


wordsElement.appendChild(newWord);

// 2. Determines which key is pressed.

document.body.onkeyup = function(event) {

    // 3. Compare onkeyup function with randomWord
    // 4. If the compare matches, the letter appears, If Else the compare matches and fills in word.  If it doesnt' letter goes into letter bank.

    // 5. If user wins/losses, the result gets logged. If user wins/losses, the game restarts.

    var userGuess = event.key;
    
    alert("You guessed " + userGuess);
}

// Reset Game:
// 1. Word Randomizies again, Guess Count Resets, Letter Bank Resets.